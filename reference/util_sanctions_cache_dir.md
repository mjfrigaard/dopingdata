# Local cache directory for USADA sanctions data

Local cache directory for USADA sanctions data

## Usage

``` r
util_sanctions_cache_dir()
```

## Value

path to the cache directory (created if missing)
