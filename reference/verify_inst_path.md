# Verify inst/ path

Verify inst/ path

## Usage

``` r
verify_inst_path(inst_path = NULL)
```

## Arguments

- inst_path:

  path string to validate as an `inst/` folder path

## Value

the validated path string, or an error if the path is not a valid
`inst/` folder path.
