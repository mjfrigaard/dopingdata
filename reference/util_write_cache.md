# Write an object to the sanctions cache

Write an object to the sanctions cache

## Usage

``` r
util_write_cache(x, file)
```

## Arguments

- x:

  object to cache

- file:

  cache file name, e.g. `"sanctions.rds"`
