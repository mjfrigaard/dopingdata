# Validate that a scraped table has the expected column names

Validate that a scraped table has the expected column names

## Usage

``` r
util_validate_cols(x, expected_cols, label)
```

## Arguments

- x:

  data.frame to check

- expected_cols:

  character vector of expected column names

- label:

  human-readable label for the table, used in the error message
