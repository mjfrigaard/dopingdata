# Check whether a cached file is missing or older than `max_age_days`

Check whether a cached file is missing or older than `max_age_days`

## Usage

``` r
util_cache_is_stale(file, max_age_days)
```

## Arguments

- file:

  cache file name

- max_age_days:

  maximum cache age, in days
