# inst_pkgs <- base::dimnames(utils::installed.packages())[[1]]
# load_pkgs <- loadedNamespaces()

# gsub(pattern = "^package:",
#   x = grep(pattern = "^package:",x = search(),value = TRUE),
#   replacement = "")

# if (isFALSE("pak" %in% base::dimnames(utils::installed.packages())[[1]])) {
#   utils::install.packages("pak",
#     repos = "https://cloud.r-project.org",
#     quiet = FALSE)
#   library(pak)
# }

options(styler.cache_root = "styler")
